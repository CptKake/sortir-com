<h1>
    <a href="{{ path('app_home') }}">
        <img src="{{ asset('images/logo.png') }}" alt="sortir">
    </a>
</h1>
<ul class="navbar-menu">
    <li class="navbar-start">
        <a class="navbar-link" href="{{ path('app_home') }}">Accueil</a>
    </li>
    <li class="navbar-item">
        <a class="navbar-link" href="{{ path('app_city') }}">Villes</a>
    </li>
    {% if app.user %}
        <li class="navbar-item">
            <a class="navbar-link" href="{{ path('app_logout') }}">
                Se déconnecter
            </a>
        </li>
        {% if is_granted('ROLE_ADMIN') %}
            <li class="navbar-item">
                <a class="navbar-link" href="{{ path('app_admin_dashboard') }}">
                    Admin Dashboard
                </a>
            </li>
        {% endif %}

    {% else %}
        <li class="navbar-item">
            <a class="navbar-link" href="{{ path('app_register') }}">
                S'inscrire
            </a>
        </li>
        <li class="navbar-end">
            <a class="navbar-link" href="{{ path('app_login') }}">
                Se connecter
            </a>
        </li>
    {% endif %}
</ul>