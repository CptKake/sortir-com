<nav class="navbar is-primary" role="navigation" aria-label="main navigation">
    <div class="navbar-brand">
        <a class="navbar-item" href="{{ path('app_main') }}">
            <img src="{{ asset('images/logo.png') }}" alt="sortir">
            <strong>Sortir.com</strong>
        </a>

        <a role="button" class="navbar-burger" aria-label="menu" aria-expanded="false" data-target="navbarMenu">
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
            <span aria-hidden="true"></span>
        </a>
    </div>

    <div id="navbarMenu" class="navbar-menu">
        <div class="navbar-start">
            <a class="navbar-item" href="{{ path('app_main') }}">
                <span class="icon"><i class="fas fa-home"></i></span>
                <span>Accueil</span>
            </a>
            <div class="navbar-item has-dropdown is-hoverable">
                <a class="navbar-link">
                    <span class="icon"><i class="fas fa-calendar-alt"></i></span>
                    <span>Sorties</span>
                </a>
                <div class="navbar-dropdown">

                    <a class="navbar-item" href="{{ path('sortie_index') }}">
                        Voir les sorties
                    </a>
                    <a class="navbar-item" href="{{ path('sortie_index') }}">
                        Mes sorties
                    </a>
                    <a class="navbar-item" href="{{ path('sortie_create') }}">
                        Créer une sortie
                    </a>
                </div>
            </div>
            {% if is_granted('ROLE_ADMIN') %}
                <div class="navbar-item has-dropdown is-hoverable">
                    <a class="navbar-link">
                        <span class="icon"><i class="fas fa-cog"></i></span>
                        <span>Administration</span>
                    </a>
                    <div class="navbar-dropdown">

                        <a class="navbar-item" href="{{ path('app_lieu_index') }}">
                            Lieux
                        </a>
                        <a class="navbar-item" href="">
                            Campus
                        </a>
                        <a class="navbar-item" href="{{ path('admin_import') }}">
                            Importer des utilisateurs
                        </a>
                        <a class="navbar-item" href="{{ path('admin_user_add') }}">
                            Créer un utilisateur
                        </a>
                    </div>
                </div>
            {% endif %}
        </div>

        <div class="navbar-end">
            {% if app.user %}
                <div class="navbar-item has-dropdown is-hoverable">
                    <a class="navbar-link">
                        <span class="icon"><i class="fas fa-user"></i></span>
                        <span>{{ app.user.nom }} {{ app.user.prenom }}</span>
                    </a>
                    <div class="navbar-dropdown is-right">
                        <a class="navbar-item" href="{{ path('app_profile_show') }}">
                            Mon profil
                        </a>
                        <hr class="navbar-divider">
                        <a class="navbar-item" href="{{ path('app_logout') }}">
                            Déconnexion
                        </a>
                    </div>
                </div>
            {% else %}
                <div class="navbar-item">
                    <div class="buttons">
                        <a class="button is-primary" href="{{ path('app_register') }}">
                            <strong>S'inscrire</strong>
                        </a>
                        <a class="button is-light" href="{{ path('app_login') }}">
                            <strong>Connexion</strong>
                        </a>
                    </div>
                </div>
            {% endif %}
        </div>
    </div>
</nav>

<script>
    document.addEventListener('DOMContentLoaded', () => {
        // Code pour le menu burger
        const burger = document.querySelector('.navbar-burger');
        const menu = document.querySelector('.navbar-menu');

        burger.addEventListener('click', () => {
            burger.classList.toggle('is-active');
            menu.classList.toggle('is-active');
        });
    });
</script>