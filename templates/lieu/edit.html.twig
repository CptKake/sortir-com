{% extends 'base.html.twig' %}

{% block title %}Modifier un lieu - {{ parent() }}{% endblock %}

{% block body %}
    <section class="section">
        <div class="container">
            <div class="columns is-centered">
                <div class="column is-8">
                    <div class="box">
                        <h1 class="title has-text-centered mb-5">Modifier le lieu</h1>

                        {{ form_start(form) }}
                            {{ include('lieu/_form.html.twig') }}
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </section>
{% endblock %}

